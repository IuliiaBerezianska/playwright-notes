
**Playwright Notes #1 — Версія 2**

**BrowserContext — чому без нього тести починають "жити чужим життям"**

Тест "залогінься як Alice" — зелений.
Тест "залогінься як Bob" — теж зелений.

Запускаєш обидва разом — і Bob раптом бачить кошик Alice.

Перша підозра: flaky тест, нестабільне CI. Справжня причина простіша: обидва тести писали cookies в одне й те саме сховище. Логін Boba переписав логін Alice — хто останній записав, той і "виграв".

Ось тут я по-іншому подивилась на BrowserContext. До цього сприймала його як аналог режиму інкогніто — не неправда, але не пояснює головного.

**Простими словами:** в Chrome можна створити кілька профілів — "Робота", "Особисте" — і в кожному свій Gmail, своя сесія, хоча Chrome встановлений один. BrowserContext — те саме, тільки програмно: не новий браузер і не новий процес, а новий ізольований профіль всередині нього.

На кожен контекст — своя копія всього, що бачиш у вкладці **Application** в DevTools: cookies, localStorage, sessionStorage, IndexedDB. Під капотом Playwright просто дає команду через CDP (той самий протокол, яким користується сама вкладка DevTools) — і браузер сам виділяє ізольований простір. Playwright нічого не зберігає сам.

Саме тому новий контекст коштує мілісекунди, а не секунди — новий браузер не запускається.

А якщо не створити контекст і відкрити дві сторінки в одному й тому самому — вони пишуть в одну cookie jar. Тому Bob і побачив кошик Alice: для браузера вони обидва — один і той самий користувач.

💡 Що змінилося в моєму розумінні
Я перестала питати "як ізолювати тест". Тепер питаю "чому взагалі щось не ізольоване за замовчуванням" — і відповідь завжди веде до одного місця: хтось поділив BrowserContext там, де мав поділити тільки Browser.

---
Ось стиснена версія — усе технічне лишила (список DevTools + CDP), а трохи "повітря" навколо прибрала:

---

**Playwright Notes #1 — Версія 2**

**BrowserContext — чому без нього тести починають "жити чужим життям"**

Тест "залогінься як Alice" — зелений.
Тест "залогінься як Bob" — теж зелений.

Запускаєш обидва разом — і Bob раптом бачить кошик Alice.

Перша підозра: flaky тест, нестабільне CI. Справжня причина простіша: обидва тести писали cookies в одне й те саме сховище. Логін Boba переписав логін Alice — хто останній записав, той і "виграв".

Ось тут я по-іншому подивилась на BrowserContext. До цього сприймала його як аналог режиму інкогніто — не неправда, але не пояснює головного.

**Простими словами:** в Chrome можна створити кілька профілів — "Робота", "Особисте" — і в кожному свій Gmail, своя сесія, хоча Chrome встановлений один. BrowserContext — те саме, тільки програмно: не новий браузер і не новий процес, а новий ізольований профіль всередині нього.

На кожен контекст — своя копія всього, що бачиш у вкладці **Application** в DevTools: cookies, localStorage, sessionStorage, IndexedDB. Під капотом Playwright просто дає команду через CDP (той самий протокол, яким користується сама вкладка DevTools) — і браузер сам виділяє ізольований простір. Playwright нічого не зберігає сам.

Саме тому новий контекст коштує мілісекунди, а не секунди — новий браузер не запускається.

А якщо не створити контекст і відкрити дві сторінки в одному й тому самому — вони пишуть в одну cookie jar. Тому Bob і побачив кошик Alice: для браузера вони обидва — один і той самий користувач.

Це перший з трьох якорів, про які я писала раніше.

💡 Що змінилося в моєму розумінні
Я перестала питати "як ізолювати тест". Тепер питаю "чому взагалі щось не ізольоване за замовчуванням" — і відповідь завжди веде до одного місця: хтось поділив BrowserContext там, де мав поділити тільки Browser.

---

Лінк на повний гайд — у першому коментарі.

---

Ось оновлений фрагмент — вставляю список одразу після пояснення "своя сесія", з прив'язкою до DevTools:

---

**Playwright Notes #1 — Версія 2**

**BrowserContext — чому без нього тести починають "жити чужим життям"**

Тест "залогінься як Alice" — зелений.
Тест "залогінься як Bob" — теж зелений.

Запускаєш обидва разом — і Bob раптом бачить кошик Alice.

Перша підозра завжди одна: flaky тест, нестабільне середовище, "щось з CI". Особливо коли проблема з'являється тільки при паралельному запуску.

Справжня причина зазвичай простіша: обидва тести писали cookies в одне й те саме сховище. Логін Boba просто переписав логін Alice — хто останній записав, той і "виграв".

Ось тут я по-іншому подивилась на BrowserContext.

До цього сприймала його як аналог режиму інкогніто. Це не неправда, але не пояснює головного — що там насправді відбувається "під капотом".

**Простими словами:**

У Chrome можна створити кілька профілів — "Робота", "Особисте" — і в кожному свій Gmail, своя історія, свої закладки, хоча встановлений Chrome один. Перемкнувся на інший профіль — ніби відкрив зовсім інший браузер, хоча фізично це той самий застосунок.

BrowserContext — це те саме, тільки програмно. Browser — один запущений процес. BrowserContext — не новий браузер і не новий процес, а новий ізольований "профіль" всередині нього. На кожен контекст — своя копія всього того, що бачиш у вкладці **Application** у DevTools: cookies, localStorage, sessionStorage, IndexedDB. Один і той самий сайт в одному контексті може бути "залогінений", а в іншому — бачити зовсім чистий стан.

Саме тому створення нового BrowserContext коштує мілісекунди, а не секунди — Playwright не запускає новий браузер, він просто просить той самий браузер відкрити новий ізольований профіль. Це і дає можливість створювати чистий контекст на кожен тест, не платячи за це швидкістю.

А якщо не створити новий контекст і просто відкрити дві сторінки всередині одного й того самого — вони буквально пишуть в одну й ту саму cookie jar. Тому Bob і побачив кошик Alice: не тому що щось "зламалось", а тому що для браузера вони обидва — один і той самий користувач.

Це і є перший з трьох якорів, про які я писала раніше.

💡 Що змінилося в моєму розумінні
Я перестала питати "як ізолювати тест". Тепер питаю "чому взагалі щось не ізольоване за замовчуванням" — і відповідь завжди веде до одного місця: хтось поділив BrowserContext там, де мав поділити тільки Browser.

---

Лінк на повний гайд — у першому коментарі.

---

Ось з місточком — цього разу коректно, бо посилається на розділ "３ базові якорі" у самому гайді, а не на пост, якого не було:

---

**Playwright Notes #1 — Версія 2**

**BrowserContext — чому без нього тести починають "жити чужим життям"**

Тест "залогінься як Alice" — зелений.
Тест "залогінься як Bob" — теж зелений.

Запускаєш обидва разом — і Bob раптом бачить кошик Alice.

Перша підозра: flaky тест, нестабільне CI. Справжня причина простіша: обидва тести писали cookies в одне й те саме сховище. Логін Boba переписав логін Alice — хто останній записав, той і "виграв".

Ось тут я по-іншому подивилась на BrowserContext. До цього сприймала його як аналог режиму інкогніто — не неправда, але не пояснює головного.

**Простими словами:** в Chrome можна створити кілька профілів — "Робота", "Особисте" — і в кожному свій Gmail, своя сесія, хоча Chrome встановлений один. BrowserContext — те саме, тільки програмно: не новий браузер і не новий процес, а новий ізольований профіль всередині нього.

На кожен контекст — своя копія всього, що бачиш у вкладці **Application** в DevTools: cookies, localStorage, sessionStorage, IndexedDB. Під капотом Playwright просто дає команду через CDP (той самий протокол, яким користується сама вкладка DevTools) — і браузер сам виділяє ізольований простір. Ці дані живуть в пам'яті браузерного процесу і зникають одразу як контекст закривається — Playwright сам нічого не зберігає, окрім єдиного явного випадку: `context.storageState()`, коли він серіалізує cookies у файл (звідси й механізм "залогінься раз, використовуй скрізь").

Саме тому новий контекст коштує мілісекунди, а не секунди — новий браузер не запускається.

А якщо не створити контекст і відкрити дві сторінки в одному й тому самому — вони пишуть в одну cookie jar. Тому Bob і побачив кошик Alice: для браузера вони обидва — один і той самий користувач.

Це один із трьох базових якорів, про які я писала в самому гайді — саме з нього, як виявилось, випливає решта.

💡 Що змінилося в моєму розумінні
Я перестала питати "як ізолювати тест". Тепер питаю "чому взагалі щось не ізольоване за замовчуванням" — і відповідь завжди веде до одного місця: хтось поділив BrowserContext там, де мав поділити тільки Browser.

📚 Повний розбір теми — https://iuliiaberezianska.github.io/playwright-notes/

---
